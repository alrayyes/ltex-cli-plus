# Security

This repo builds a CI-only image with no runtime secrets or exposed
services. If you find a real vulnerability anyway - something in the build
process, not a bug in `ltex-ls-plus` itself, which belongs upstream - report
it privately via [GitHub's private vulnerability reporting](https://github.com/alrayyes/ltex-cli-plus/security/advisories/new)
rather than opening a public issue.
